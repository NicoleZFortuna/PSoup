# A function to pull the final states from a set of simulations.

A function to pull the final states from a set of simulations.

## Usage

``` r
finalStates(simulations)
```

## Arguments

- simulations:

  a list containing the output of the setupSims function
