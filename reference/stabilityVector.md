# A function to create a vector that reports whether simulations have managed to reach stability by the maximum timestep.

A function to create a vector that reports whether simulations have
managed to reach stability by the maximum timestep.

## Usage

``` r
stabilityVector(allSim)
```

## Arguments

- allSim:

  the output list of setupSims
