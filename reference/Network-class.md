# A function to generate objects of class 'Network'

A function to generate objects of class 'Network'

## Slots

- `name`:

  the name of the network.

- `model`:

  a list of length 2 with the names Hormones and Genotypes
