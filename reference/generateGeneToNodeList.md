# a function to map the genes that effect nodes in a network

a function to map the genes that effect nodes in a network

## Usage

``` r
generateGeneToNodeList(network)
```

## Arguments

- network:

  and object of class network
